// Atividade 3

let numero = parseInt(prompt("Digite um número para fatorar."))
 
function calcularFatorial (fatorial) {
    let num = 1;
    for (let i = num; i > 1; i--) {
        resultado *= i;
    }
     return resultado;
}
console.log(calcularFatorial(fatorial));